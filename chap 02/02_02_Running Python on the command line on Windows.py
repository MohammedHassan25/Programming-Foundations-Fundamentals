# ------------------------------------------------------------------------------
# Syntax is The rules of a programming language . It must be right
# exit() is a quit on command line
# You can run the code through the command line, but it is better to run it in IDE
# ------------------------------------------------------------------------------
